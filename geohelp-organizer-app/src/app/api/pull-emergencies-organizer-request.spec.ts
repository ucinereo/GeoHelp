import { PullEmergenciesOrganizerRequest } from './pull-emergencies-organizer-request';

describe('PullEmergenciesOrganizerRequest', () => {
  it('should create an instance', () => {
    expect(new PullEmergenciesOrganizerRequest()).toBeTruthy();
  });
});
