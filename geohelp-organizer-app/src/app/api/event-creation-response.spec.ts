import { EventCreationResponse } from './event-creation-response';

describe('EventCreationResponse', () => {
  it('should create an instance', () => {
    expect(new EventCreationResponse()).toBeTruthy();
  });
});
